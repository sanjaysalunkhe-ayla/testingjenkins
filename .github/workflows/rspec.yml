name: Jenkins Integration

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  trigger-jenkins-job:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        uses: sanjaysalunkhe-ayla/testingjenkins@master
        with:
          jenkins_url: 'https://9dd3-209-133-48-2.ngrok-free.app'
          job: 'git-demo'
